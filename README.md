# LibraryManagementAPI

This project is the backend for the Library Management System. It is built using .NET 9 and follows a multi-layer architecture with an emphasis on separation of concerns.

## Development Server

To start a local development server, run:

```bash
dotnet run
